# Full local-dev / all-features install (README's `pip install -r requirements.txt`).
# Two other manually-maintained subsets exist for different deploy targets —
# check both when adding/removing a dependency here:
#   requirements.runtime.txt — lean default used by Dockerfile (no NLP/embedding stack)
#   requirements.pi.txt      — Raspberry Pi 5 arm64 build (drops torch/sentence-transformers/onnxruntime)
fastapi
uvicorn
pydantic
cryptography
httpx
presidio-analyzer
presidio-anonymizer
spacy==3.7.0
https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.7.0/en_core_web_lg-3.7.0.tar.gz
sqlalchemy
psycopg2-binary
pgvector
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers
llama-cpp-python
onnxruntime
