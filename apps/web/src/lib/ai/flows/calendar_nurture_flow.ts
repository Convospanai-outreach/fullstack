/**
 * Calendar Nurture Flow Shell
 * Migrated to backend.
 */
export const calendarNurtureFlow = {
    run: async (_input: any) => { throw new Error("Genkit flows are only available on the backend."); }
};
