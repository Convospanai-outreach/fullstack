export const metadata = {
    title: "Contact Us | CraftMyFunnel",
    description: "Get in touch with the CraftMyFunnel team.",
};

export default function ContactLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
