export default function SectionTitle({ title }: { title: string }) {
    return (
        <h2 className="text-4xl font-bold text-center my-10 text-purple-400">
            {title}
        </h2>
    );
}
