# Persistent Open-Items Ledger

**Last Reconciled:** 2026-08-13 (OPEN-23 through OPEN-31 added/updated: PR #106 conflict check, audit of five untracked working-directory folders, branch protection enabled + live-verified via PR #157, 4 new Dependabot alerts investigated, one real bug + one pre-existing CI bug found and fixed)

---

## Current Priority Order (2026-08-13)

Reprioritized across all currently non-resolved items, most urgent+important first:

1. **OPEN-21** — Real secrets (Neon `DATABASE_URL`/`DIRECT_URL`, `ENCRYPTION_KEY`, `GOOGLE_CLIENT_SECRET`) printed to a transcript on 2026-08-09, still not rotated. Urgent: standing exposure, 4+ days open. Important: credential compromise risk. **User confirmed 2026-08-13: still not rotated.**
2. **OPEN-28** — edge-fastapi and apps/api share one DB for two unsynced "Workflow" representations by default; needs a topology decision (separate DB per edge node vs. documented shared use), not urgent but should be decided before more edge nodes come online.
3. **OPEN-03 / OPEN-04** — Deferred, blocked on external human action (Google CASA, Azure AD portal) — not actionable further without the user driving those portals directly.
4. **PR #157 not yet merged** — this session's entire diff (OPEN-17, 23-31) sits on `chore/open-items-ledger-and-edge-endpoints`, all four required gates green. Merging is the user's call, not automated by this session (see OPEN-17's own history of merges landing without confirmation).

**Resolved this session:** OPEN-17 (branch protection enabled AND live-verified via PR #157 — see OPEN-29), OPEN-23 (PR #106 closed as superseded; rate limiting + legacy-key auto-upgrade reimplemented on #110's design), OPEN-24 (Chrome extension auth — Clerk-backed token minting, CORS, expiry/active-membership checks), OPEN-25 (edge-fastapi missing endpoints — code-complete but not runtime-verified, see row for caveat), OPEN-26 (reframed — `/queue/*` vs `/extension/tasks/*` are not duplicates), OPEN-27 (deleted all four dead untracked folders + orphaned `apps/api/routes/queue/`), OPEN-29 (branch protection confirmed working end-to-end on a real PR — all four gates reported `pass`), OPEN-30 (4 new Dependabot alerts investigated: 2 already-accepted per OPEN-13, 2 were false positives from a stray corrupted `apps/api/package-lock.json`, now deleted), OPEN-31 (the branch-protection test run itself caught one real bug in this session's own OPEN-24 work — a static DB import that would have broken the build — plus one unrelated pre-existing Docker-tag bug in the Phi-3 verify script; both fixed).

**Catalogued, not actioned:** two new untracked design-handoff folders (`Color and type pairings/`, `CraftMyFunnel UI System/`) and untracked `oracle-vm-deployment.md` (OPEN-08's evidence doc, recommend committing) — see OPEN-27 for detail. Not cleanup candidates.

---

## Open & Resolved Items

| ID | Item | Category | Status | Details / Evidence Requirement |
|----|------|----------|--------|--------------------------------|
| **OPEN-01** | Multi-Tenant Data Isolation | Security | **Resolved** | Verified write (`updateMany`), read (`findFirst`), and delete (`deleteMany`) paths enforce `teamId` scoping. Commit [`a8972f1`](https://github.com/Convospanai-outreach/fullstack/commit/a8972f1): Live test `scripts/test-tenant-isolation.mjs` executed against Neon DB confirmed Team A update/read/delete on Team B lead returns 0 rows modified. `getCurrentContext()` derives `teamId` from server-verified Clerk/NextAuth JWT; `convo-workspace-id` cookie is membership-validated before use. |
| **OPEN-02** | Remote GitHub Actions CI Execution | CI/CD | **Resolved** | Commit [`7556801`](https://github.com/Convospanai-outreach/fullstack/commit/7556801): Remote GitHub Actions runs `#30882600074` (`CI`), `#30882600084` (`Production Readiness Gate`), and `#30882600257` (`Vercel Parity Build`) all completed with `status: "completed"` and `conclusion: "success"`. |
| **OPEN-03** | Google OAuth CASA Assessment | Compliance | **Deferred** | Human action required in Google Cloud Console / CASA portal. |
| **OPEN-04** | Azure AD App Registration (Microsoft OAuth) | Integrations | **Deferred** | Human action required in Azure Portal. |
| **OPEN-05** | Live Gmail RFC 5322 Message-ID Reply Detection | Verification | **Resolved** | **2026-08-09:** Abandoned the planned live send→reply cycle after recognizing it can't discriminate: `createInboundCampaignEvent` matches on `threadId` **OR** `providerId`, and a reply lands in the same Gmail thread regardless of whether `providerId` is correct — so a same-thread live test would pass identically whether or not OPEN-16's Message-ID fix worked, proving nothing. Replaced with two targeted unit tests that together prove the actual claim: (1) the existing send-side test confirms Gmail's wire `Message-ID` header is captured and stored as `Email.providerId`; (2) new test `"matches an inbound reply by wire providerId alone, independent of threadId"` (`googleMailboxService.test.ts`) sets the inbound message's `threadId` to a value that matches no stored `Email` row and asserts `createInboundCampaignEvent`'s `email.findFirst` OR-clause still resolves the match via `providerId` sourced from the `In-Reply-To` header — and that the sync still reports `replies: 1`. This isolates providerId-based matching from threadId-based matching, which a live cycle structurally cannot do. 106/106 tests passing in this file. No production data was written; the local `apps/api` instance run against production Neon/Google credentials for this investigation was shut down and its plaintext `.env` deleted (see note below on credential exposure). **Operational caveat (cross-ref OPEN-08):** this resolves the code-level correctness question only. Checked all three entry points into `syncGoogleMailbox` — the job-queue handler (`gmail-history-sync-worker.ts`), the Pub/Sub push handler (`handleGooglePubSubNotification`), and the polling fallback (`worker-manager.ts`) — all three live exclusively under `apps/api/src/workers/`, run only by `start-workers.ts`. `handleGooglePubSubNotification` is not wired to any Fastify route in the current tree (no live webhook endpoint), so there is no push path independent of the worker either. Since OPEN-08 confirms no worker process is deployed anywhere on Render, this entire reply-detection pipeline — Message-ID matching included — is currently unreachable in production, not just partially degraded. |
| **OPEN-06** | Dashboard Stat Cards Database Backing | Audit | **Resolved** | All four primary stat cards use real `teamId`-scoped Prisma queries — no hardcoded fakes. Commit [`ed87f97`](https://github.com/Convospanai-outreach/fullstack/commit/ed87f97) (`apps/web/src/app/api/dashboard/summary/route.ts`). Full audit: [`dashboard_stat_cards_audit.md`](file:///C:/Users/tewar/.gemini/antigravity-ide/brain/fce46043-36d6-4142-bd27-d2bf09b2763f/dashboard_stat_cards_audit.md). |
| **OPEN-07** | Accessibility Page Audit | UX/QA | **Resolved** | Automated axe-core 4.13.0 audit run against live authenticated sessions for `/intel`, `/scraper-bridge`, `/jobs`. Full results: [`docs/audits/accessibility-intel-scraper-bridge-jobs.md`](docs/audits/accessibility-intel-scraper-bridge-jobs.md). One critical violation (`select-name` on the Scrape Target field) fixed directly (`apps/web/src/modules/scraper-bridge/ui/components/ScrapeForm.tsx` — associated the label via `htmlFor`/`id`). Seven remaining moderate/serious findings (color-contrast, landmark structure) documented in the audit file as follow-up work requiring design/layout decisions outside this pass's scope. |
| **OPEN-08** | Production Worker Process Monitoring | Operations | **Resolved** | **2026-08-11:** Deployed for real — `apps/api` now runs on two self-managed Oracle Cloud VMs (`api-main` at `api.craftmyfunnel.live` running the HTTP server, `api-worker` running `src/workers/start-workers.ts` via Docker Compose with `restart: unless-stopped`), per `oracle-vm-deployment.md`. Both point `DATABASE_URL`/`DIRECT_URL` at the existing managed Neon project and `REDIS_URL` at existing managed Redis (no self-hosted DB on the VMs). `apps/web`'s `NEXT_PUBLIC_API_URL`/`API_INTERNAL_ORIGIN` (Vercel) repointed from Render to `api.craftmyfunnel.live`; live production traffic (Google Pub/Sub mailbox push, real dashboard load) confirmed hitting the new API post-cutover. Render's `fullstack` service (`srv-d971fb4s728c738grijg`) is now suspended (not deleted). The worker's first-ever live production run (it had never actually executed before, on Render or anywhere) immediately surfaced two independent pre-existing bugs in the LinkedIn `VISIT` sequence step: (1) `apps/api/Dockerfile`'s final stage never copied Puppeteer's downloaded Chromium out of the build stage, so the browser could never launch — fixed by installing Alpine's own `chromium` package + `PUPPETEER_EXECUTABLE_PATH`; (2) `sequenceHandlers.ts` called `runLinkedInAction` with `{ type, url, message }` instead of the function's actual `{ action, profileUrl, loginToken }` signature, producing `Executing undefined on undefined` and a hard failure at `page.goto(undefined)` on every VISIT/CONNECT/MESSAGE job — fixed at all three call sites. Both fixed and merged in [PR #149](https://github.com/Convospanai-outreach/fullstack/pull/149); worker confirmed stable for 6+ hours post-fix with the job-execution crash-loop gone. Separately, the previously-unconfirmed Neon dual-project question is now resolved as a side effect: the live `DATABASE_URL` from Render's dashboard resolved to `ep-rough-mud-a137oo29-pooler.ap-southeast-1.aws.neon.tech`, confirming `convospan`/`proud-bird-55226596` (ap-southeast-1) is the production project; `late-union-39485411` remains unused and was not touched. Earlier investigation, retained for context: Commit [`0b23df5`](https://github.com/Convospanai-outreach/fullstack/commit/0b23df5): local supervision now defined — `apps/docker-compose.split.yml` runs the worker (`src/workers/start-workers.ts`) with `restart: unless-stopped`. **2026-08-09, confirmed live via authenticated Render MCP (`list_services`, workspace `tea-d1200emmcj7s73eqb9n0`):** the Render account has exactly 3 services, all `type: web_service`, all built from `apps/api/Dockerfile` — production `fullstack` (`srv-d971fb4s728c738grijg`, branch `main`, https://fullstack-vz1l.onrender.com) plus two ephemeral PR-preview copies (`fullstack PR #110`, `fullstack PR #106`). **There is no worker/background-worker/cron service anywhere in the account.** The `apps/docker-compose.split.yml` worker service from `0b23df5` is not deployed anywhere — this is no longer a hypothesis. Any production code path that assumes a background worker is alive (campaign sends, Gmail history sync, sequence follow-ups, the stale-job watchdog) is currently dead in prod. **Real topology, also newly confirmed:** web frontend is a separate Vercel project (`fullstack-web-xkxn`, team `convo2026s-projects`), invisible from Render-only access. **Action needed (human decision):** deploy the worker — either a second Render Background Worker service running `src/workers/start-workers.ts` (the compose file already gives the exact command to reuse) on the same repo/image, or migrate to a managed queue (Inngest/Trigger.dev) to avoid a second always-on process on a free/starter Render plan. **Separately flagged, unconfirmed:** two Neon Postgres projects exist under the same org — `convospan` (`proud-bird-55226596`, ap-southeast-1) shows real usage (~65MB data, `compute_last_active_at` current as of 2026-08-09, 83k+s historical active compute) and is almost certainly the one `dbFactory.ts` resolves for the live `DATABASE_URL`; `late-union-39485411` (us-east-2) has zero active/compute time since creation on 2026-03-16 (`compute_last_active_at` == `created_at`) — never used. The Render MCP tools available only support *writing* env vars (`update_environment_variables`), not reading them, so which Neon project `DATABASE_URL`/`UAE_DATABASE_URL` on the live Render service actually points at was not verified directly — confirm via the Render dashboard env vars before deciding whether `late-union-39485411` is an intentional unused standby (e.g. for an unlaunched UAE region) or dead configuration to decommission. |
| **OPEN-09** | Gmail PubSub / History Inbound Reply Processing Engine | Architecture | **Resolved** | Commit [`8a921a4`](https://github.com/Convospanai-outreach/fullstack/commit/8a921a4): Consolidated solely into `apps/api`'s canonical, OIDC-verified, lease-locked pipeline (`registerGoogleMailboxWatch`, `/integrations/google/pubsub` Fastify route, `gmail-history-sync-worker.ts`, `syncGoogleMailbox`). Redundant unauthenticated implementation in `apps/web` (`syncGmailInboundReplies` and `/api/integrations/google/pubsub/route.ts`) deleted per GEMINI.md §5 single source of truth rules. HTTP 400 Bad Request handling ported to `apps/api` alongside 404 for stale history ID profile resync fallback. All 282 API tests and 154 Web tests passing. |
| **OPEN-10** | Reply Event Type Mismatch (apps/api vs apps/web) | Data Integrity | **Resolved** | Commit [`8a921a4`](https://github.com/Convospanai-outreach/fullstack/commit/8a921a4): `apps/api` `createInboundCampaignEvent` wrote event type `"REPLY"` while web consumers expected `"REPLY_RECEIVED"`. Unified `apps/api` `GmailInboundEventType` and `assertCompletedInboundEvent()` check to `"REPLY_RECEIVED"`, aligned dashboard summary and funnel route filters. Redundant `apps/web` duplicate route and tests deleted. All 282 API unit tests passing (100%). |
| **OPEN-11** | `apps/api` Coverage Threshold Drift | Code Quality | **Resolved** | Was resolved at 49.07% via commit `7556801`, then a separate commit (`30b372a`) broke `npm ci` itself (see OPEN-16), so no CI run reached the coverage step for a window and the gate regressed unnoticed to `lines 48.84%` / `statements 47.98%` (both below threshold) by the time `npm ci` was fixed. **2026-08-08:** Ruled out a `prisma` version float as the cause (confirmed `prisma@7.9.1` predates all of this session's lockfile edits). Direct diff of `apps/api/src` between the two commits showed net source *shrinkage*, not growth, so the drift wasn't new untested code — it was `src/lib/sequenceService.ts` (13 statements, real branching logic for the VISIT→CONNECT→MESSAGE→EMAIL sequence) sitting at 0% coverage the whole time, on a margin thin enough (~1-6 lines) that any small shift tipped it under. Fixed on PR #140 branch, commit `ab57c62`: added real unit tests for all three `SequenceService` methods and all four `scheduleNextStep` branches. Verified locally: lines 48.84%→49.19%, statements 47.98%→48.31%, exit code 0. |
| **OPEN-12** | Webhook Delivery Timeout & SSRF Exposure | Security | **Resolved** | Commit [`76f84d7`](https://github.com/Convospanai-outreach/fullstack/commit/76f84d7): the `timeout: 10000` option was a no-op under undici (carried over from node-fetch), so a hung endpoint could occupy a worker indefinitely — replaced with `AbortSignal.timeout()`. User-supplied webhook URLs could probe internal infrastructure; `assertSafeWebhookUrl` now resolves the hostname and blocks loopback, RFC1918, CGNAT, link-local (incl. `169.254.169.254`), and IPv6 ULA/link-local via `net.BlockList`, checking resolved addresses (not the literal hostname) so a public DNS record can't point at an internal address. Webhook tests: 3 → 14. |
| **OPEN-13** | Dependency Advisories (Lockfile Overrides Not Applied) | Security | **Resolved** | Commit [`30b372a`](https://github.com/Convospanai-outreach/fullstack/commit/30b372a): the `overrides` block was never applied to the committed lockfile (`npm ci` — used by CI and both Dockerfiles — kept installing the vulnerable tree). Corrected stale pins (postcss, fast-uri, hono), added missing pins (find-my-way, ip-address, socket.io-parser, valibot), dropped the blanket brace-expansion pin, and regenerated the lockfile from a clean tree. `npm audit`: 19 vulnerabilities (10 high) → 0. **2026-08-08:** Two new advisories disclosed after that fix regressed the gate again: `dompurify` GHSA-55q2-fjhq-7xh7 (XSS) and `js-yaml` GHSA-5p4m-2wfm-xmqj / CVE-2026-59870 (DoS). Patched on PR #140 branch (`8b574df`): `dompurify` 3.4.12 → 3.4.13, `js-yaml` 4.3.0 → 4.3.1 (root `package.json` overrides). A third advisory, `image-size` (pulled in transitively via `pptxgenjs@4.0.1`, GHSA-w3rx-r6r6-pgpr + GHSA-5p2g-fcmc-qvqq, DoS, `range: "<=2.0.2"` — no patched version exists at all), was deliberately **not** patched — the only remedy is downgrading `pptxgenjs` 4.0.1 → 1.1.5, breaking the live PPTX-export feature. Traced every `pptxgenjs.addImage()` call in the codebase (one call site, `apps/web/src/modules/studio/presentationDeck.ts`): it only ever passes a synthetic, in-process-generated SVG data URI, never an ICNS/JXL/HEIF file or anything user-supplied — the vulnerable parser paths are unreachable in this app's actual usage. Allowlisted on PR #140 branch (`3c9c0b8`, `scripts/audit-with-allowlist.mjs`) with the reasoning above inline, rather than breaking the feature or disabling the gate. PR #140 merged to `main` at `54ceeb4`. **2026-08-08 (later):** A separate, unrelated Dependabot-style merge (`994bc1c`, PR #115) landed on `main` with `find-my-way@9.6.0` despite the root override already pinning `9.7.0` (`GHSA-c96f-x56v-gq3h`, HTTP2 DDoS) — same "override not applied to lockfile" defect class, this time with a clean non-breaking fix available. That commit's lockfile was also out of sync with its own `package.json` (missing `@tailwindcss/oxide-*` entries — `npm ci` was broken on `main` again). Both fixed in PR #141. |
| **OPEN-14** | npm audit CI Gate Non-Blocking | CI/CD | **Resolved** | Commit [`df90374`](https://github.com/Convospanai-outreach/fullstack/commit/df90374): the audit step ran with `\|\| true`, so it could never fail a build. Now blocking; verified `npm audit --audit-level=high --omit=dev` exits 0 against the tree at the time. The destructive-migration scan is deliberately left advisory (34 findings, 4 critical, in migrations already applied to production — needs an allowlist for applied history before it can gate). **2026-08-08:** The new `image-size` advisory (OPEN-13) regressed this gate; fixed alongside it via `scripts/audit-with-allowlist.mjs`, which still fails the build on any advisory not explicitly allowlisted (kept meaningfully blocking rather than reverting to `\|\| true`). |
| **OPEN-15** | Deactivated/Invited Users Retaining Team Context | Security | **Resolved** | Commit [`2c00d25`](https://github.com/Convospanai-outreach/fullstack/commit/2c00d25): `getCurrentContext()` matched team membership on `userId`(+`teamId`) with no status filter, so a deactivated user holding a live session retained full team context (same for invited-but-never-joined rows). `apps/api` was fully exposed — it has no Clerk code path, so every request resolved context through these lookups. All four lookups (both apps) now constrain to `status: "active"`; regression tests added, verified to fail without the fix. Also removed unreachable `CredentialsProvider`/Google provider dead code from `apps/api` authOptions (verified nothing reads `authOptions.providers` there — apps/api is Clerk-only via a proxy to apps/web). |
| **OPEN-16** | `npm ci` Broken (Missing `uuid@14.0.1` in Lockfile) | CI/CD | **Resolved** | Commit [`9ab6c2c`](https://github.com/Convospanai-outreach/fullstack/commit/9ab6c2c) (PR #139): a prior lockfile edit left `uuid@14.0.1` (pulled in via the root `overrides` pin) missing from the `apps/api`/`apps/web` workspace `node_modules/uuid` entries, so `npm ci` failed with "Missing: uuid@14.0.1 from lock file" and blocked every CI job and both Docker builds. Regenerated with `npm@10.9.8` (matching CI's Node 22.x-bundled npm — local npm 11.6.3 resolves workspace overrides differently and produces a lockfile CI can't consume) and hand-verified the diff was a pure 52-line addition. Unintended side effect: `apps/api`'s `"prisma": "^7.8.0"` caret range let this regeneration float the resolved `prisma`/`@prisma/*` family to `7.9.1` — noted for anyone chasing OPEN-11. |
| **OPEN-17** | ~~Untraced Auto-Merge Automation~~ — Reframed | CI/CD / Security | **Resolved — branch protection enabled 2026-08-13** | An earlier session flagged PR #138 and PR #139 as merged by an untraced automation within ~30-60s of push. **2026-08-08:** Checked `gh pr view --json mergedBy,autoMergeRequest` and the PR timeline API for both: `mergedBy` is the user's own account (`Convospanai-outreach` / Siddharth Tewari, not a bot), `autoMergeRequest` is `null` (GitHub's native auto-merge was never armed), and the actual elapsed time from push to merge was ~9.5 minutes (PR #138) and ~10.5 hours (PR #139) — not 30-60 seconds. No webhook, GitHub App installation, or workflow step calling the merge API was found (`gh api repos/.../hooks` returns `[]`; no branch protection). This is consistent with ordinary manual merges by the user rather than automation, and the original 30-60s claim doesn't hold up under evidence — but it isn't proof positive either; a merge under the user's own PAT (e.g. from a script or a different session) would look identical in this evidence. Don't over-read this as a closed case. Practical implication unchanged either way: if you're mid-fix on a branch, expect it may get merged before a follow-up commit lands — land fixes as single, pre-verified commits when possible. **2026-08-08 (later):** PR #140 (created 06:41:45, merged 09:14:07 — ~2.5h, same account) was merged while `Web Build (apps/web)` was still showing `fail`. That's weak evidence against careful human review before merge, and it confirms the operationally load-bearing fact regardless of who/what is merging: `main` has **no branch protection**, so nothing requires checks to be green before a merge lands. Any red check found after the fact (like OPEN-18 below) can already be on `main`. **2026-08-12:** The pattern recurred live during OPEN-22's fix — PR #154 merged at 10:50:30Z, before a follow-up push (adding aggregator gate jobs) landed at 11:06:20Z, silently dropping that commit from the merge; the orphaned commit was cherry-picked onto a fresh branch (PR #155), and PR #155 itself was also merged externally within seconds of its CI going green, before this session could merge it manually. **Groundwork laid, protection not yet enabled:** `ci.yml`, `production-gate.yml`, and `vercel-parity-build.yml` each split into a docs-only path and a real-build path via mutually exclusive `if:` conditions with no job that always runs — GitHub does not treat "skipped" as satisfying a required status check, so naively requiring either job name would deadlock every future PR (a code PR leaves "Docs-only Validation" stuck skipped forever; a docs-only PR leaves the real build stuck skipped forever). Added a "CI Gate" / "Production Gate" / "Vercel Parity Gate" aggregator job to each (mirroring `verify.yml`'s pre-existing "Merge Gate" pattern — `if: always()`, evaluates whichever path ran), merged to `main` via PR #155 (commit `b8775a6`), verified green. **2026-08-13 (closed):** User approved via explicit confirmation. Ran `gh api -X PUT repos/Convospanai-outreach/fullstack/branches/main/protection` with `required_status_checks: {strict: true, contexts: ["CI Gate", "Production Gate", "Vercel Parity Gate", "Merge Gate"]}`, `enforce_admins: true`, no required PR reviews, no push restrictions. Response confirmed: all four contexts registered, force-pushes and branch deletion now blocked. Merges to `main` now require those four checks to pass (and be up to date with `main`, `strict: true`) before merge is possible, closing the "PR merges before follow-up commits land" pattern documented above. |
| **OPEN-18** | Web Build Failure: `@prisma/client` Resolution Is Structurally Incompatible Between Root Scripts and `apps/web`'s `prisma generate` | CI/CD | **Resolved** | `Web Build (apps/web)`'s "Live DB Concurrency Verification" step runs `scripts/test-db-concurrency.mjs` (repo-root-relative) from the repo root under a `--workspace apps/web`-scoped `npm ci`. That script imports `@prisma/client`, `pg`, and `@prisma/adapter-pg` directly. **Investigated and empirically confirmed both directions of a hard constraint — not fixable via lockfile/package.json edits:** (1) When `apps/web` keeps its own nested `apps/web/node_modules/@prisma/client` (today's `main` state), `apps/web`'s own `npx prisma generate --schema prisma/schema.prisma` succeeds — Prisma's CLI resolves the client relative to the workspace that owns the schema and does not walk up to a monorepo root. But the root-level script then fails (`Cannot find package '@prisma/client'` or, if a stale root copy exists, `SyntaxError: Named export 'PrismaClient' not found` against a non-generated base package). (2) When root and `apps/web` are made to resolve to the exact same `@prisma/client` version (tried: declaring it as an explicit root dependency, pinned exact to match `apps/web`'s `7.8.0`), npm's hoisting dedupes to a single shared root copy — the root script's plain `require`/`import` now finds it, but `prisma generate` then fails with `Could not resolve @prisma/client. Please try to install it with npm i @prisma/client...`, from *both* `apps/web` and repo-root working directories. Tried a `--legacy-peer-deps` full lockfile regeneration under both configurations; the outcome was consistent across clean reinstalls, so this isn't a flake. **Candidate fixes (none applied — this is a decision, not a bug fix):** (a) drop the "Live DB Concurrency Verification" step from the `--workspace apps/web`-scoped job and run it in a job that does a full unscoped install instead; (b) move `scripts/test-db-concurrency.mjs` and its siblings (`test-tenant-isolation.mjs`, `db-seed.ts`, `verify-schema.mjs`, `create-test-user.ts`, ...) into `apps/web/scripts/` so they resolve `@prisma/client` the same way `apps/web`'s own code does; (c) have the CI step copy/symlink `apps/web`'s already-generated client into root `node_modules` immediately before running the script. (b) looks the most durable but is a repo-layout change, so it's left for a human call rather than made unilaterally. The `find-my-way` CVE fix and lockfile-sync fix originally bundled with this investigation have been split out — see OPEN-13/14. **2026-08-08 (later):** PR #141 merged (`2a0abd6`) *before* the revert commit reverting the broken root-dependency change landed — the same "no branch protection, merges land mid-fix" pattern documented in OPEN-17. `main` briefly carried the broken state live, confirmed via a red `Web Build` run ([31268842104](https://github.com/Convospanai-outreach/fullstack/actions/runs/31268842104), same `SyntaxError: Named export 'PrismaClient' not found` as above). The revert (this entry's actual fix) is [PR #143](https://github.com/Convospanai-outreach/fullstack/pull/143), branched fresh off `main` after the merge, cherry-picking the already-verified revert commit. **2026-08-09:** Option (b) implemented. Checked which of the "sibling" scripts are actually invoked anywhere before moving anything (per surgical-changes practice) — only `scripts/test-db-concurrency.mjs` (`ci.yml`'s "Live DB Concurrency Verification" step, no `working-directory` override) was affected; `create-test-user.ts` looked like a second instance of the same bug but isn't — `playwright.yml` already invokes it with `working-directory: apps/web`, which resolves to `apps/web/scripts/create-test-user.ts`, a newer and more complete version (uses the shared `prisma` singleton from `apps/web/src/lib/db`, includes the OPEN-15 `status: "active"` fix) — the *root* `scripts/create-test-user.ts` is a stale, unreferenced duplicate, left in place per "don't delete unrelated dead code" rather than removed as part of this fix. The other four scripts flagged as siblings (`test-tenant-isolation.mjs`, `db-seed.ts`, `verify-schema.mjs`, `verify-ml-training.ts`, `create-audit-user.ts`) aren't invoked by any workflow or `package.json` script at all — left untouched. Moved `scripts/test-db-concurrency.mjs` to `apps/web/scripts/test-db-concurrency.mjs` (self-contained, no imports from sibling scripts, and `apps/web/package.json` already depends on `pg`/`@prisma/adapter-pg`) and added the missing `working-directory: apps/web` to its `ci.yml` step. Verified: `npm ci --workspace apps/web --include-workspace-root --legacy-peer-deps` (clean) + `prisma generate`, then running the moved script from `apps/web` gets past module resolution entirely and fails only on "no local Postgres to connect to" (a real DB connectivity error, not `Cannot find package '@prisma/client'`) — confirming the fix. Fixed and merged via [PR #147](https://github.com/Convospanai-outreach/fullstack/pull/147) (commit [`d344971`](https://github.com/Convospanai-outreach/fullstack/commit/d344971), 2026-08-10) — `apps/web/scripts/test-db-concurrency.mjs` confirmed present at the new path on `main`. |
| **OPEN-19** | apps/api Gmail Send Path Missing RFC 5322 Message-ID (production divergence from apps/web) | Data Integrity | **Resolved** | Discovered while scoping OPEN-05's live test: `apps/web`'s `googleMailboxService.ts` (302-352) generates a `Message-ID:` header, sends it, then re-fetches the message to capture Gmail's wire value — but `apps/api`'s independent copy of the same function did neither, storing Gmail's internal `data.id` as `Email.providerId` instead. Confirmed via `render.yaml` (worker service `dockerCommand` runs `apps/api/src/workers/start-workers.ts`) that **production's send path is apps/api's copy**, not apps/web's — so OPEN-05's "code-level wiring is correct in both apps" was inaccurate for the path production actually executes. Also confirmed `createInboundCampaignEvent` (apps/api, 1301-1311) matches inbound replies on `threadId` OR `providerId`, so a same-thread reply would have matched via `threadId` alone even with a broken `providerId` — the live send→reply test alone would not have caught this. Fixed by porting the same Message-ID generation + post-send wire-fetch into `apps/api`'s `sendViaGmailMailbox` (reusing its existing `fetchGmailMessage`/`headerValue` helpers rather than duplicating apps/web's raw fetch). Two tests updated, one added (`googleMailboxService.test.ts`) to cover the wire-fetch success and fallback-to-generated-id paths; all 298 `apps/api` tests pass. The pre-existing two-implementation split between apps/web and apps/api itself is not addressed here — flagged for a future consolidation pass, out of scope for this fix. |
| **OPEN-20** | Render `apps/api` Missing `RESEND_API_KEY` | Configuration | **Resolved** | System-notification emails (`apps/api/src/lib/notifications.ts`, wired up 2026-08-09) silently no-op when `RESEND_API_KEY` is unset — the client is only constructed `if (process.env["RESEND_API_KEY"])`. The key existed in Vercel (`apps/web`) but Vercel flags it `Sensitive`, so it can't be retrieved via CLI/API (confirmed via `vercel env ls` and a redacted `vercel env pull`, same restriction hit earlier for `GOOGLE_CLIENT_SECRET`/`ENCRYPTION_KEY`). User retrieved the value from the Vercel dashboard directly and supplied it; set on Render's only deployed service (`fullstack`, `srv-d971fb4s728c738grijg` — confirmed via `list_services` to be the sole service in the workspace, a `web_service` built from `apps/api/Dockerfile`; no separate worker service exists, consistent with OPEN-08) via the Render MCP `update_environment_variables` tool in merge mode (existing vars untouched). Deploy `dep-d9sat2v10e5c739j3jeg` triggered automatically to pick up the change. |
| **OPEN-21** | Real Secrets Briefly Printed to Assistant Transcript During OPEN-05 Investigation | Security | **Open — human action required** | While diagnosing a duplicate-env-var bug in a locally-created `apps/api/.env` (populated with real Render production values to test the Gmail send path), a `grep -n` command printed full matching lines — including the real Neon `DATABASE_URL`/`DIRECT_URL`, `ENCRYPTION_KEY`, and `GOOGLE_CLIENT_SECRET` — into tool output that became part of the conversation transcript. Caught immediately and all subsequent checks switched to length/prefix-only inspection; no further exposure occurred. The local `apps/api` dev server was stopped and the `.env` file containing these values was deleted (2026-08-09), but the values already appeared in this session's transcript, which is not a secure secret store. **Recommend rotating the Neon database password and the Google OAuth client secret** (Google Cloud Console → APIs & Services → Credentials) and updating Render's env vars accordingly. This is a judgment call for the user/team, not something to do unilaterally. |
| **OPEN-22** | CI "Docs-only Change Detection" Job Fails on Back-to-Back Merges (`no merge base`) | CI/CD | **Resolved** | **2026-08-12:** PR #153's CI failed in the `Docs-only Change Detection` job with `fatal: origin/main...HEAD: no merge base` (exit 128), cascading to skip `Web Build`, `API Strict Typecheck`, and `Docker Build Smoke` entirely (never ran) and showing `Merge Gate` as FAILURE — the PR merged anyway per OPEN-17 (no branch protection). Root cause: the "Classify changed files" step re-fetched the base ref with `git fetch --no-tags --prune --depth=1 origin "${{ github.base_ref }}"` immediately before diffing — the `--depth=1` truncated `origin/<base_ref>` to a single commit. When `main` had advanced past the PR branch's actual fork point (as it had here, since PR #152 merged first and moved `main`'s tip forward before PR #153's CI ran), the subsequent three-dot diff (`git diff origin/main...HEAD`) couldn't compute a merge-base against that single fetched commit and hard-failed. Same identical job (copy-pasted) existed in four workflow files. **Fix (branch `fix/ci-shallow-fetch-merge-base`):** dropped only the `--depth=1` flag (kept the fetch itself, since relying on `actions/checkout@v4`'s `fetch-depth: 0` alone to have already populated an up-to-date `origin/<base_ref>` ref was an unverified assumption not worth resting a fix on) in all four workflow files — `ci.yml`, `production-gate.yml`, `vercel-parity-build.yml`, `verify.yml`. Also added `|| changed=""` to the diff line so that under `set -e`, a future diff failure fails *open* (empty `changed` → `docs_only=false` → all jobs run) instead of killing the step and silently skipping every downstream build, which is what actually made #153's failure invisible at merge time. **Verified:** [PR #154](https://github.com/Convospanai-outreach/fullstack/pull/154) — all four `Docs-only Change Detection` jobs passed in 6-13s (no `no merge base` error), and correctly cascaded into passing `Web Build (apps/web)` (3m22s), `API Strict Typecheck (apps/api)` (1m51s), `Docker Build Smoke` (1m49s), and `Merge Gate`, confirming both that the classification step no longer breaks and that a real PR's downstream jobs run to completion. |
| **OPEN-23** | PR #106 Conflicts With Merged PR #110 — Competing API-Key Implementation | Security / Process | **Resolved** | **2026-08-13:** PR #110 (`fix(security): integrate api key issuance and auth`) merged to `main` at `aeecbca`, storing keys as `cmf_sha256_v1:<last4>:<sha256(rawToken)>`. PR #106 (`fix(security): harden api key storage and scopes`, open since 2026-07-10) independently built a competing design storing keys as `v2:<sha256(rawKey)>:<lastFour>`, plus its own `apiKeyService.ts` persistence layer, key-management rate limiting (10 req/min per user on create/list, 60 req/min per key digest on auth), and automatic legacy-key upgrade-to-v2 on successful authentication — none of which #110 implements (#110 explicitly deferred legacy-key rewriting). Verified via `git merge-tree $(git merge-base origin/main pr-106-head) origin/main pr-106-head`: **31 conflict-marker hunks across 13 files**, all in the exact surface #110 rewrote (`apps/api/src/lib/apiAuth.ts`, `apiKeySecurity.ts`, `apps/api/routes/settings/keys/*`, `apps/api/routes/governance/keys/*`, `apps/web/src/app/(dashboard)/governance/keys/page.tsx`, `apps/web/src/app/settings/keys/page.tsx`). Only two files were genuinely new with no main-side equivalent: `apps/api/src/lib/apiKeyService.ts` and its test. Merging #106 as-is would have reintroduced a second, incompatible key-hash format alongside the one just shipped. **User decision:** close #106, reimplement fresh. **Closed** [PR #106](https://github.com/Convospanai-outreach/fullstack/pull/106) with an explanatory comment pointing to this entry. **Reimplemented both capabilities on top of #110's merged design** (branch `chore/open-items-ledger-and-edge-endpoints`): (1) **Key-management rate limiting** — added a `KEY_MANAGEMENT` tier (10 req/min per user) to the existing `apps/api/src/lib/rateLimit.ts` (`applyRateLimit`/Redis-with-LRU-fallback module — chosen over the three other rate-limiting modules already in the codebase, `apiRateLimit.ts`/`rate-limit.ts`/`rateLimitService.ts`, since it's the only one supporting a per-call custom limit and is already the pattern for user-identified tiers), applied to GET/POST on both `apps/api/routes/settings/keys/route.ts` and `apps/api/routes/governance/keys/route.ts` (create/list only, matching #106's original scope — not the `[id]` delete route). Not covered by an automated test: `applyRateLimit` deliberately no-ops when `NODE_ENV === "test"` (`rateLimit.ts:251`), matching the existing untested convention for every other caller of this module in the codebase (`v1/leads`, `v1/campaigns`, etc.) — confirmed no route using this module has a 429 test today. **Caveat surfaced, not hidden:** on `/v1/*` API-key routes, `authorizeApiKey`'s new 60/min-per-key limit (below) stacks *under* the pre-existing 100/min-per-team limit in `apiRateLimit.ts` — a single-key team's effective ceiling on those routes is now 60/min, not 100/min. (2) **Legacy-key auto-upgrade-on-auth** — `apps/api/src/lib/apiAuth.ts`'s `authorizeApiKey` now rewrites a legacy key's DB row from plaintext to a hashed digest the first time it authenticates successfully (`keyRecord.key === presentedKey` on the plaintext lookup branch is the signal it hasn't been upgraded yet). Caught before implementation: the obvious approach (reuse `cmf_sha256_v1:<last4>:<hash>`, #110's stored-digest format for *new* keys) would have made every upgraded legacy key satisfy `STORED_API_KEY_DIGEST_PATTERN` and read back with `legacy: false` in both key-list UIs — the exact signal admins need to know which keys still need rotating would go dark the moment each key is used. Fixed by giving upgraded legacy keys their own digest prefix, `cmf_legacy_sha256_v1:<original cs_live_/sk_live_ prefix>:<last4>:<hash>` (`createUpgradedLegacyApiKeyValue` in `apiKeySecurity.ts`), so `getStoredApiKeyDisplayMetadata` keeps returning `legacy: true` and the original prefix after the upgrade, with no schema migration needed. `getApiKeyLookupValues` for a legacy-format presented key now returns both the plaintext and the possible-upgraded-digest form, so a key authenticates correctly whether or not it's been upgraded yet (upgrade is lazy/on-demand, not a batch migration). Also added the 60 req/min-per-key-digest auth-side limiter, extending `apiAuth.ts`'s existing in-file bucket machinery rather than importing a second rate-limit module into the same function (that module's `applyRateLimit` no-ops under `NODE_ENV === "test"`, so it can't be unit-tested the way the existing bucket machinery already is) — keyed by `keyRecord.id` so both legacy lookup forms of the same row share one bucket, and deliberately independent of the pre-existing failed-auth-only throttle in the same file. **Verified:** `apps/api/src/lib/apiKeySecurity.test.ts` and `apiAuth.test.ts` updated (legacy lookup now asserts a 2-element array) plus 4 new tests (upgrade digest format/round-trip, upgrade-on-auth fires once and not on an already-upgraded row, 60-req/min per-key throttle trips and returns 429 with `Retry-After`); full `apps/api` suite (41 files, 351 tests) passes; `tsc --noEmit` clean except one pre-existing, unrelated error in `services/browser/browser-engine.ts` (confirmed via `git diff` — not touched by this change). |
| **OPEN-24** | Chrome Extension Sends One Credential for Two Required Auth Headers | Security / Integration | **Resolved (backend + routing) — token-acquisition UI still pending** | **2026-08-13:** Audit of untracked `chrome-extension-private` (found to be empty/irrelevant — see OPEN-27) led to auditing the real, tracked extension at `apps/api/src/extension` instead. `apps/api/routes/extension/_lib/auth.ts` requires two distinct credentials on every request: `x-extension-key` must equal env `EXTENSION_API_KEY` (a shared secret), and `Authorization: Bearer <token>` must be a valid `Session.sessionToken` looked up in Prisma. The extension's popup only exposes a single "API token" field (`popup.js:71,235,536`) and `background.js` (lines 212, 242) sends that same one value for both headers. A user's extension key cannot double as a valid session token, so authenticated backend sync (`/api/extension/leads/capture`, `/api/extension/leads/{id}/linkedin-contacted`) will fail as currently wired — this is a real, unresolved integration gap, not cosmetic. The routing itself is correct: the extension's endpoint paths match `apps/api/routes/extension/leads/capture/route.ts` and `.../leads/[id]/linkedin-contacted/route.ts` exactly, proxied via `apps/web/src/app/api/extension/[...path]/route.ts`. **Fix options:** add a dedicated session-token field to the extension's popup/options UI, or change `validateExtensionAuth` to accept a single extension-scoped credential instead of requiring both. **2026-08-13 (continued):** Before implementing, found the "add a field" option isn't a clean bug fix — it conflicts with the extension's own declared approval scope. `background.js`'s header comment (lines 1-9) states "This worker does not crawl LinkedIn, read credentials, collect session data... " and `CMF_FEATURES.backendLeadSync` is explicitly `false` (line 13). But that flag is dead: it's declared once, returned as-is by the unused `CMF_GET_FEATURES` handler, and never read by `popup.js` or anything else — `savePreparedLead` → `trySyncLead` (`background.js:181,207`) fires unconditionally on every saved lead regardless of the flag. So backend sync is already live in the shipped V1 extension despite the code's own contract saying it should be off, and it's failing (per this entry) because of the missing second credential. Separately confirmed via `manifest.json:18-19,22` that only `background.js`/`popup.html` are wired as the live extension — the already-correct two-credential pattern (`token` vs `extensionKey`) exists in `options.html:19-26`, `options.js:15-16`, `background.v2-planned.js:161-162`, none of which are referenced by the manifest (same dormant V2 scaffold as OPEN-26). Adding a live-session-token paste field to V1's popup would mean the extension starts holding a full web-session bearer credential in `chrome.storage.local`, a materially bigger permission/approval surface than the shared extension-key it holds today — a store-review-relevant decision, not a mechanical fix. **Blocked on user decision**, options: (a) accept the scope expansion and add the session-token field to V1 (`popup.html`/`popup.js`/`background.js`), matching the pattern already built for V2; (b) treat backend sync as V2-only work — gate `trySyncLead` behind `CMF_FEATURES.backendLeadSync` (currently dead) so V1 correctly stays local-only as documented, and move authenticated sync to the V2 scaffold once that's promoted; (c) redesign `validateExtensionAuth` to accept one extension-scoped, per-user credential instead of the current shared-secret + session-token pair (larger backend change, no popup UI change needed). **User decision:** "fix the auth to actually work with Clerk sessions." **2026-08-13 (fixed):** Before implementing, discovered the auth check was broken for *any* caller, not just the extension: `resolveUserId` looked up `Bearer <token>` against `Session.sessionToken`, but `apps/web/src/lib/auth.ts` sets NextAuth `session: { strategy: "jwt" }` — under JWT strategy NextAuth never writes to the `Session` table — and real login is via Clerk, which doesn't touch that table either. The only existing token-mint endpoint (`apps/api/routes/extension/auth/token/route.ts`) depended on the same non-existent NextAuth session and additionally produced a `rawToken.expiresAt` two-part token format that `_lib/auth.ts`'s own `isValidShortLivedToken` (expects a 3-part `header.payload.signature` JWT shape) would have rejected anyway — three independent, compounding bugs, not one. Also found the extension's proxy (`apps/web/src/app/api/extension/[...path]/route.ts`) sets no CORS headers and has no `OPTIONS` handler, so a browser would block the preflight/response for any cross-origin `chrome-extension://` request regardless of auth — sync was failing at the network layer before auth logic ever ran. **Fix (branch `chore/open-items-ledger-and-edge-endpoints`):** (1) Added a real Clerk-authenticated mint endpoint, `POST /api/extension/token` (`apps/web/src/app/api/extension/token/route.ts`), using the existing `findOrCreateClerkAppUser()` (`apps/web/src/lib/clerkAuth.ts`) — the same Clerk verification every other authenticated apps/web route already relies on — to mint a 15-minute opaque token stored as a `Session` row (that table's shape is a correct, unused-elsewhere opaque-token store; the bug was never in the table, only in what populated it). The old broken mint endpoint at `apps/api/routes/extension/auth/token/route.ts` is left in place (had zero callers before and after — not deleted per surgical-changes practice) but is now superseded; flagged here for a future cleanup pass. (2) Removed the dead `isValidShortLivedToken`/dotted-token parsing from `apps/api/routes/extension/_lib/auth.ts` (it validated a token shape nothing produces) and added an explicit `session.expires > new Date()` check, since the prior lookup never checked expiry at all — an expired row would have authenticated forever. (3) Added `status: "active"` to the membership select in the same function, matching the OPEN-15 fix pattern already applied to ~15 other membership lookups in this codebase — `validateExtensionAuth` was resolving `teamIds` from every membership row including deactivated/invited ones, unfiltered. (4) Added CORS handling (`Access-Control-Allow-Origin` echoed only for `chrome-extension://` origins, plus an `OPTIONS` handler) to the extension proxy route. (5) Split the extension's popup single "API key/token" field into two — "Extension key" (`x-extension-key`, unchanged shared secret) and "Sync token" (the new Clerk-minted `Authorization: Bearer` token) — across `popup.html`, `popup.js`, `background.js`. **Verified:** new test file `apps/api/routes/extension/_lib/auth.test.ts` (5 tests: extension-key gate, missing/invalid/expired token, active-membership scoping) passes; existing extension route tests (12 tests, 4 files) still pass; `tsc --noEmit` clean on both `apps/api` and `apps/web` for all touched files. **Not yet done, flagged for follow-up:** no UI button exists yet in the web app to call `POST /api/extension/token` and hand the user a copyable value — today a technical user must call it directly (e.g. via browser devtools while logged in). A "Generate extension token" button (e.g. on `apps/web/src/app/setup/page.tsx`'s existing extension section) is a small, separate UX task, not bundled into this auth-correctness fix. Also unresolved: `background.js`'s own header comment ("does not... collect session data") and the still-dead `CMF_FEATURES.backendLeadSync: false` now describe a scope the shipped code has moved past — the user's decision resolved the auth mechanism, not this documentation/flag mismatch; left as-is per the original scope-conflict finding. **2026-08-13 (live-tested via curl, one more real bug found and fixed):** Booted the apps/web dev server and curl-tested the actual request path rather than trusting the unit tests alone. Found a fourth, independent, and the most fundamental blocker: `apps/web/src/proxy.ts` (the actual Next.js middleware — confirmed live via `x-clerk-auth-status` response headers, despite being named `proxy.ts` not `middleware.ts`; see commit `2879ca9` history) has its own blanket auth gate requiring a Clerk cookie or NextAuth JWT for any non-whitelisted `/api/*` path. `/api/extension/*` was never whitelisted, so **every** extension sync request — old or new credential scheme, doesn't matter — was being 401'd by this gate before ever reaching `apps/web`'s `[...path]` proxy route, let alone `apps/api`. This was true before any of today's changes too; it's the actual reason sync "fails," more fundamental than the credential-mismatch this entry started from. Confirmed via live curl against a local dev server (`POST /api/extension/leads/capture` before fix: `{"error":"Unauthorized"}` from the middleware itself; after fix: request correctly reaches the proxy route's forwarding logic). **Fix:** added `extensionApiPrefixes = ["/api/extension"]` to `proxy.ts`, exempting it from the blanket cookie/JWT gate the same way `authApiPrefixes`/`webhookApiPrefixes` already are — safe because every real handler under `apps/api/routes/extension/*` (confirmed via grep: `action`, `leads/capture`, `leads/[id]/linkedin-contacted`, `leads`, `push`, `tasks/*`, `auth/validate`) already independently calls `validateExtensionAuth`; `/api/extension/token` keeps its own independent Clerk check via `findOrCreateClerkAppUser()` regardless of the middleware exemption. Also found `proxy.ts`'s own OPTIONS/CORS handling unconditionally intercepts every `/api/*` preflight before any nested route handler runs, so the OPTIONS handler added earlier today to `[...path]/route.ts` was dead code; excluded `/api/extension/*` from that middleware-level interception too so the nested handler's chrome-extension-aware CORS logic actually executes (verified via curl: preflight to `/api/extension/leads/capture` from a `chrome-extension://` origin now returns 204 with the correct `Access-Control-Allow-Origin`/`-Headers`). **Also addressed (pre-empting review, not yet-discovered bugs):** the mint endpoint's 15-minute TTL was unusable — `background.js` has no re-mint/refresh path, so the sync token is a standing credential, not a one-time paste; extended to 30 days (still expires, unlike the field it replaces, which never did) with a comment noting the missing refresh flow. Grepped every `prisma.session.*` call site (4 total, all extension-code) to confirm nothing else in the app — no cookie-based lookup, no `PrismaAdapter` session-read path under the `jwt` strategy — would treat a minted extension token as a live web session if leaked; confirmed isolated. **Not covered by an automated test:** the `proxy.ts` middleware fix (no existing test harness for this file); verified manually via curl against a local dev server instead, both before and after the fix, for both the auth-gate bypass and the CORS/OPTIONS behavior. |
| **OPEN-25** | `apps/edge-fastapi` Missing Endpoints Already Called by `apps/api` in Production | Architecture / Production Bug | **Resolved (unverified at runtime)** | **2026-08-13:** `apps/api/src/services/HardwareService.ts` (tracked, active) calls `${EDGE_NODE_URI}/workflows/save`, `GET /workflows`, and `/compliance/mode` on the edge-fastapi service with `x-api-key` auth. Tracked `apps/edge-fastapi/main.py` had **no such routes** — confirmed via full comparison against the untracked `convospan-edge-fastapi` folder, a standalone split (own tiny local git repo, one commit, file mtimes Feb 17–Mar 22 2026, pre-dates the ConvoSpan→CraftMyFunnel rename on 2026-05-28) that does have `WorkflowRecord`/`EdgeSetting` models and `POST /workflows/save`, `GET /workflows`, `POST /compliance/mode`. These calls 404'd wherever the edge service is actually deployed. **Did not port the untracked implementation wholesale** — it predates two hardening passes on tracked `apps/edge-fastapi` (`e1c182f` 2026-04-15 "Infrastructure Hardening: Fixed 33 vulnerabilities", `fef4696` 2026-06-01 "harden launch gate and edge runtime") and would have regressed security if copied as-is: no `require_edge_api_key`/`hmac.compare_digest` auth on any route, `/execute` (browser-control actuator) unconditionally enabled instead of gated by `EDGE_EXECUTE_ENABLED`, PII stored unencrypted (tracked uses Fernet via `_encrypt_pii`/`EDGE_VAULT_KEY`, `enc:v1:` prefix), and `critique_response` fails **open** (`"APPROVED"`, score 1.0, on model/cold-start failure) vs. tracked's fail-closed `"REJECTED"`. **Fixed** (commit [`6b2b869`](https://github.com/Convospanai-outreach/fullstack/commit/6b2b869)): reimplemented `WorkflowRecord`, `EdgeSetting`, and the three endpoints in `apps/edge-fastapi/main.py` + `database.py`, layered on the hardened base (all three routes gated via `require_edge_api_key`; `GET /workflows` intentionally left unscoped by `team_id` per the edge node's single-shared-key trust model — see in-file docstring). **Verification caveat — not resolved to the same bar as other items on this ledger:** fastapi/sqlalchemy aren't installed in this environment, so the change was checked via `ast.parse` (syntax only) and manual read against the existing authenticated `/search` endpoint pattern, not by actually running the service or exercising the routes. Treat as code-complete, not runtime-verified; a real smoke test against a live edge node (or CI once fastapi deps are available there) is still owed before fully trusting this closes the 404 gap. Also surfaces a second, separate architectural concern — split out as **[[OPEN-28]]** rather than buried in this row. |
| **OPEN-28** | edge-fastapi and apps/api Have Two Unsynced Stores for the Same "Workflow" Concept | Architecture | **Open — flagged, not fixed** | **2026-08-13:** Surfaced while implementing OPEN-25. `apps/api` already has a Prisma `Workflow`/`WorkflowRun` model; edge-fastapi's new `WorkflowRecord`/`EdgeSetting` tables (OPEN-25) are a second, independent representation of the same concept. In `docker-compose.split.yml`'s default wiring, edge-fastapi's `DATABASE_URL` points at the *same* Postgres database apps/api uses. Table names don't collide (`Workflow` vs `workflows`) so nothing breaks today, but it's one DB silently holding two unsynced sources of truth for "workflow" state. Likely intended resolution is a separate database per edge node (each physical device's local workflow cache, distinct from apps/api's canonical record) — but that's a topology decision, not something to change unilaterally. **Fix:** either give each edge node its own DB in the default compose wiring, or, if a shared DB is intentional, document why the two schemas are deliberately kept separate. |
| **OPEN-26** | ~~Duplicate Extension Task-Queue Backend Implementations~~ — Reframed | Code Quality / Architecture | **Reframed — `/queue/*` is orphaned, not competing; deletion folded into OPEN-27** | **2026-08-13:** Original framing assumed `apps/api/routes/queue/*` and `apps/api/routes/extension/tasks/*` were two competing implementations of the same feature needing a canonical-vs-duplicate decision. Investigated and that premise doesn't hold: **they are not duplicates.** Different Prisma models (`queue/*` reads/writes `agentTask` + `agentLog`; `extension/tasks/*` reads/writes `job` + `systemEvent`) and different task vocabularies (`queue/*`: `VISIT/CONNECT/MESSAGE/INMAIL`, direct LinkedIn browser-automation actions; `extension/tasks/*`: `OPEN_PROFILE/ADD_LEAD/INSERT_DRAFT/LOG_MANUAL_LINKEDIN_ACTION`, matching the current CraftMyFunnel-assistant vocabulary also used by `extension-bridge.ts` and `extension/action/route.ts`'s `VALID_ACTIONS`). Chronology confirms which is legacy: `queue/pending/route.ts` added `f564c53` (2026-03-23); `extension/tasks/pending/route.ts` added `893356c` (2026-06-08, commit titled "Repurpose extension as CraftMyFunnel assistant") — `queue/*` predates the product pivot that made its LinkedIn-automation task model obsolete (server-side automation now lives in `linkedin-runner`/`puppeteerRunner.ts`, out of scope here). **Consumer check:** grepped both apps for every caller — `/extension/tasks/*` has exactly one consumer, the dormant V2 scaffold `background.v2-planned.js:179,334,347` (not wired into `manifest.json`); `/queue/*` has zero consumers anywhere in tracked code (its own route + test file are the only matches for `context.mode === "BROWSER"`, the field it filters on). **Producer check, with an open caveat:** no tracked code creates `agentTask` rows with `context.mode === "BROWSER"`, and no tracked code creates `job` rows with the task types `extension/tasks/pending` polls for — both queues are unfed today as far as static analysis shows. Caveat: `AgentExecutor.ts:77` and `orchestrator/swarm/run/route.ts:152` both create `agentTask` rows with a caller-supplied `context` blob whose origin wasn't traced further, so a live-DB row count (not grep) is the only thing that would fully rule out an in-flight or historical `mode: "BROWSER"` row. **Also worth noting:** `/queue/*` is the more hardened of the two (claim-token + TTL concurrency control, `PIIScrubber` on result payloads, idempotent conflict handling on duplicate results) — worth keeping as a reference implementation if `/extension/tasks/*` is ever promoted alongside V2, not a reason to prefer it as canonical today. **Smaller finding, not fixed:** `extension/tasks/complete/route.ts` duplicates the job-completion logic already present in `extension/tasks/result/route.ts`, and nothing calls `/complete` at all. **Disposition:** no code change here — `apps/api/routes/queue/` (dead, orphaned, superseded by the product pivot) is added to OPEN-27's deletion list so its removal waits for the same user confirmation as the other stray-code cleanup, rather than being deleted unilaterally on this session's own read of "no producer found in git." |
| **OPEN-27** | Untracked Working-Directory Folders + Dead Tracked Route — Fully Cataloged, Recommend Deletion | Housekeeping | **Resolved — deleted 2026-08-13** | **2026-08-13:** User flagged three untracked folders sitting alongside the repo (`chrome-extension-private`, `convospan-edge-fastapi`, `fullstack-security-sequencing-finalization`) as possibly containing unfinished work belonging in `main`. Audited all three via parallel Explore agents with git-based comparison: (1) `chrome-extension-private` — `src/`/`scripts/`/`assets/` are all empty (0 files); its only content, `README.md`, describes an unrelated tool ("Vibe Bridge") referencing a different machine's paths, not this project. The real, working LinkedIn extension already lives tracked at `apps/api/src/extension` (see OPEN-24, OPEN-26 for its actual gaps). (2) `convospan-edge-fastapi` — superseded except for the two endpoints now captured and scoped in OPEN-25; everything else in tracked `apps/edge-fastapi` is strictly newer/more hardened. (3) `fullstack-security-sequencing-finalization` — a corrupted/partial extraction (`apps/web` replaced by 0-byte garbage files, `apps/api` missing `src/`/`services/`/`workers/` entirely, 294 files vs. tracked's 1,210); its bundled docs (`PENDING_ITEMS.md` etc.) describe an unrelated Feb-2026 product snapshot, not sequencing work; the one sequencing-adjacent file present is byte-identical to `main`'s version. `main`'s git history shows continuous, active sequencing work through 2026-08-13 — this snapshot is behind, not ahead. A fourth untracked folder, `fullstack-security-apikey-core` (surfaced via glob search during OPEN-24 test-writing, contains its own copies of `apps/api/routes/extension/*` test files), is flagged here but **not yet audited** the way the other three were. **2026-08-13 (later):** OPEN-26's investigation added a fifth item to this list, but a *tracked* one — `apps/api/routes/queue/` (`pending/route.ts`, `result/route.ts`, `__tests__/legacy-queue-auth.test.ts`): confirmed orphaned end-to-end (no producer, no consumer anywhere in tracked code — see OPEN-26 for the full chronology and evidence), superseded by the 2026-06-08 extension repurposing. Unlike the three untracked folders, this is live code in `apps/api`, so its removal is a real diff, not a working-directory cleanup — still folded into this same confirm-before-delete item rather than acted on unilaterally. **2026-08-13 (fullstack-security-apikey-core audited):** its `apps/api/src/lib/apiKeySecurity.ts` uses the same `cmf_live_` presented-key prefix and `cs_live_`/`sk_live_` legacy-pattern lineage now on `main` — **not** PR #106's competing `v2:<sha256>:<lastFour>` design, so no format conflict. It's simply an earlier draft of the same lineage: missing `STORED_API_KEY_DIGEST_PREFIX`/`LEGACY_STORED_API_KEY_DIGEST_PREFIX` and the whole upgrade-on-auth machinery OPEN-23 later added to `main`. Its own `worktree` git pointer is broken (`fatal: not a git repository`, referencing a different machine's path `D:/Convo/fullstack/...`), and `IMPLEMENTATION_SUMMARY_2026-02-10.md` dates it to the same Feb-2026 snapshot family as `fullstack-security-sequencing-finalization` (2,610 files) — behind `main`, not ahead, same disposition as the other three: safe to delete, nothing to port. **OPEN-25's port-what's-needed precondition is now met** (OPEN-25 fixed, see above), clearing the last blocker on deleting `convospan-edge-fastapi`. **Two more untracked items surfaced this session, not part of the original three-folder audit and not recommended for deletion:** `Color and type pairings/` (design-handoff export, 5 files, `design_handoff_craftmyfunnel_landing/`) and `CraftMyFunnel UI System/` (design-handoff export, 3 files, `design_handoff_unified_dashboard/`) — small, recent design deliverables (CSS/theme/HTML source, no code), read as the user's active working material rather than a stray snapshot. Catalogued here for visibility; disposition (keep, move into the repo, or archive elsewhere) left to the user. Also noted: `oracle-vm-deployment.md` (repo root, untracked) is the cited evidence document for OPEN-08's "Resolved" status — evidence for a resolved item currently lives outside version control; recommend committing it rather than leaving it as an untracked file that could be lost. **2026-08-13 (closed):** User approved deletion of all five. Removed `chrome-extension-private/`, `convospan-edge-fastapi/`, `fullstack-security-sequencing-finalization/`, `fullstack-security-apikey-core/` (untracked, `rm -rf`) and `apps/api/routes/queue/` (tracked, `git rm -r`, committed separately) — re-grepped for any remaining `routes/queue` references across `apps/` first and found none. The two design-handoff folders (`Color and type pairings/`, `CraftMyFunnel UI System/`) and `oracle-vm-deployment.md` were deliberately left untouched — different disposition (user's active material / evidence doc to commit), not cleanup candidates. |
| **OPEN-29** | Branch Protection (OPEN-17) Live-Tested via PR #157 — First Real PR Since Enabling | CI/CD | **Resolved — confirmed working** | **2026-08-13:** OPEN-17 enabled `required_status_checks` on `main` (`strict: true`, `enforce_admins: true`, contexts `["CI Gate", "Production Gate", "Vercel Parity Gate", "Merge Gate"]`) without ever observing a live PR run under it — a real risk, since GitHub treats a `skipped` job as not satisfying a required context, and with `enforce_admins: true` a name mismatch would deadlock every future PR with no admin override. Pre-flight check before opening a PR: confirmed via `gh pr checks 155` (the most recent real merged PR, pre-dating this session's protection change) that all four job names appear as literal, exact check-run names and reported `pass`. **Opened [PR #157](https://github.com/Convospanai-outreach/fullstack/pull/157)** as the first live test. **Confirmed via `gh pr checks 157`:** all four required contexts report real conclusions — `CI Gate: pass`, `Production Gate: pass`, `Vercel Parity Gate: pass`, `Merge Gate: pass`. No deadlock, no stuck-pending context, `enforce_admins: true` never had to be tested against a bad state because none arose. Branch protection works as configured. (This same PR run is also what surfaced OPEN-31's real bug — the gate did exactly the job it was enabled for.) **One residual gap, not chased further:** every run on PR #157 classified as a code change (`docs_only=false`, since the branch touches `.ts`/`.sh`/lockfile files) — the docs-only path through the aggregator jobs' `if: always()` logic was never actually exercised live. Per OPEN-17's own text, a docs-only PR is precisely the scenario a naive required-check config can deadlock on. If a future docs-only PR's gates hang instead of passing, that means the `if: always()` isn't firing on that branch — check there first. |
| **OPEN-30** | 4 New GitHub Dependabot Alerts After This Session's Push — 2 Already-Accepted, 2 False Positives From Dead File | Security | **Resolved** | **2026-08-13:** Pushing this session's branch surfaced "4 vulnerabilities (3 high, 1 moderate)" via GitHub's post-push notice. Investigated all four via `gh api repos/.../dependabot/alerts`: (1) `GHSA-w3rx-r6r6-pgpr` and (2) `GHSA-5p2g-fcmc-qvqq` (both `image-size`, high, root `package-lock.json`) are the **same two advisories OPEN-13 already investigated and accepted as risk** — confirmed present verbatim in `scripts/audit-with-allowlist.mjs`'s `ALLOWLIST` map with the original OPEN-13 justification (unpatched, but only reachable via a synthetic in-process SVG string, never user-supplied input). No new information, no action needed. (3) `GHSA-jmr9-qjv8-65gv` (`extract-zip`, high) and (4) `GHSA-frvp-7c67-39w9` (`@hono/node-server`, medium) were flagged against `apps/api/package-lock.json` specifically — investigated and found **both false positives against a dead file**: this is an npm-workspaces monorepo (`package.json`'s `"workspaces"` includes `apps/api`); the *only* lockfile anything actually installs from is the root `package-lock.json` — confirmed via `apps/api/Dockerfile` (`COPY package.json package-lock.json ./` at repo root, then `npm ci --workspace apps/api --include-workspace-root`) and every CI install step across `ci.yml`/`production-gate.yml`/`vercel-parity-build.yml`/`playwright.yml`/`web-prisma-migrate.yml` (all root-driven `npm ci --workspace ... --include-workspace-root` or plain root `npm ci`; the `working-directory: apps/api` steps in `ci.yml` only run `typecheck`/`test`/`build` against already-installed `node_modules`, never install). `extract-zip` doesn't even appear in the root lockfile — not a real dependency of the running app. `@hono/node-server`'s flagged vulnerable range (`< 1.19.15`) doesn't apply to what's actually installed — root lockfile already resolves `2.1.0`. The stray `apps/api/package-lock.json` itself had drifted into **invalid JSON** (`node -e "require('./apps/api/package-lock.json')"` throws `Unexpected non-whitespace character after JSON` — the file mixes npm lockfileVersion 1's nested-`dependencies` format with v2/v3's flat `packages` format mid-file, evidence of a botched merge). `git log` on the file shows it was repeatedly touched by Dependabot's own `"bump the npm_and_yarn group across N directories"` PRs (`994bc1c`, `c60d61b`, `10b7f49`) treating it as a live manifest for years despite nothing ever installing from it. **Fixed:** deleted `apps/api/package-lock.json` (commit `1beb8dd`) — removes the dead file generating phantom alerts entirely; root lockfile (the one actually used) is untouched and unaffected. `apps/web` has no equivalent stray lockfile (checked). |
| **OPEN-31** | PR #157's First Live Branch-Protection Run Caught One Real Bug in This Session's Own Work + One Pre-Existing, Unrelated CI Bug | CI/CD | **Resolved** | **2026-08-13:** Investigating OPEN-29's live check run on PR #157, `Web Build (apps/web)` and `Production Stability Audit (apps/web)` both failed on the same cause: `apps/web/src/app/api/extension/token/route.ts` (added earlier this session for OPEN-24) had a static top-level `import { prisma } from "@/lib/db"`, which violates an existing, working guard (`scripts/check-web-route-db-imports.mjs`, run by `ci.yml`) requiring Next.js route handlers to import the DB client at request time, not at module scope, so it can't be touched during the Next.js build. Every other route handler in the codebase already follows the request-time pattern (`const { prisma } = await import("@/lib/db")`, e.g. `apps/web/src/app/api/admin/invites/route.ts`) — this was a straightforward miss when the file was written, not a design disagreement. **This is exactly the failure mode OPEN-17 exists to catch**: pre-branch-protection, this bug would have merged to `main` silently (no gate required these checks to pass). Fixed by matching the existing pattern (commit `8e78713`); `node scripts/check-web-route-db-imports.mjs` now passes clean. **Checked the guard's real reach before trusting it:** it's a regex scan of `route.ts` files' own text only — `findOrCreateClerkAppUser` (still statically imported in the fixed file) itself statically imports `prisma` from `@/lib/db` at module scope in `apps/web/src/lib/clerkAuth.ts`, invisible to the scanner. Confirmed this is a pre-existing, already-accepted condition, not a gap this fix leaves open: `apps/web/src/app/api/admin/invites/route.ts` — the exact file used as the reference pattern — already has the same transitive import and is already merged, CI-green code, along with 6 other routes using the same helper. Not new scope; the guard was written to catch a narrower class of mistake than "any DB touch," and this fix matches its actual, working convention. Separately, in the same PR run, `Phi-3 Runtime Evaluation` (a different, non-required workflow — `phi3-runtime-verify.yml`, not one of the four OPEN-17 gate contexts) failed for an unrelated pre-existing reason: `scripts/verify_ci.sh` hardcodes `IMAGE_NAME="CraftMyFunnel-verify"`, and Docker requires lowercase repository names (`ERROR: failed to build: invalid tag ... repository name must be lowercase`) — this job could never have passed, on this PR or any other, until fixed. Lowercased to `craftmyfunnel-verify` in the same commit. Not part of the four required gate contexts, so it wasn't blocking merges, but it's a permanently-broken CI job that had gone unnoticed. **Not fully verified end-to-end:** the tag fix got the job past the immediate failure (build now proceeds to compiling llama.cpp + downloading a ~2.4GB Phi-3 model, a 10-30 minute step); this job was still `in_progress` as of this writing and wasn't chased to completion since it's non-required and out of scope for this session's task. If it fails later in that pipeline for a different reason, that's a separate, still-open finding — check `gh api repos/Convospanai-outreach/fullstack/actions/jobs/94542854868` for its eventual conclusion. |
