# Persistent Open-Items Ledger

**Last Reconciled:** 2026-08-08

---

## Open & Resolved Items

| ID | Item | Category | Status | Details / Evidence Requirement |
|----|------|----------|--------|--------------------------------|
| **OPEN-01** | Multi-Tenant Data Isolation | Security | **Resolved** | Verified write (`updateMany`), read (`findFirst`), and delete (`deleteMany`) paths enforce `teamId` scoping. Commit [`a8972f1`](https://github.com/Convospanai-outreach/fullstack/commit/a8972f1): Live test `scripts/test-tenant-isolation.mjs` executed against Neon DB confirmed Team A update/read/delete on Team B lead returns 0 rows modified. `getCurrentContext()` derives `teamId` from server-verified Clerk/NextAuth JWT; `convo-workspace-id` cookie is membership-validated before use. |
| **OPEN-02** | Remote GitHub Actions CI Execution | CI/CD | **Resolved** | Commit [`7556801`](https://github.com/Convospanai-outreach/fullstack/commit/7556801): Remote GitHub Actions runs `#30882600074` (`CI`), `#30882600084` (`Production Readiness Gate`), and `#30882600257` (`Vercel Parity Build`) all completed with `status: "completed"` and `conclusion: "success"`. |
| **OPEN-03** | Google OAuth CASA Assessment | Compliance | **Deferred** | Human action required in Google Cloud Console / CASA portal. |
| **OPEN-04** | Azure AD App Registration (Microsoft OAuth) | Integrations | **Deferred** | Human action required in Azure Portal. |
| **OPEN-05** | Live Gmail RFC 5322 Message-ID Reply Detection | Verification | **Still Open** | Empirical live send → reply cycle test needed. Code-level wiring is correct in both `apps/web` (post-send wire header fetch) and `apps/api` (full `In-Reply-To`/`References` matching in `createInboundCampaignEvent`). Needs one live cycle to confirm Gmail preserves the wire Message-ID. **2026-08-07:** Confirmed one `ConnectedMailbox` row exists (`tewari.85@gmail.com`, `status: CONNECTED`, refresh token present, access token expired 2026-08-03 — will auto-refresh on next use). Did not trigger a live send: this requires choosing a real recipient and sending a real email from a real account, which needs explicit human authorization before each attempt, not something to automate silently. Next step is a human-supervised cycle: send one real campaign email through the app, reply to it, confirm `createInboundCampaignEvent` threads it. |
| **OPEN-06** | Dashboard Stat Cards Database Backing | Audit | **Resolved** | All four primary stat cards use real `teamId`-scoped Prisma queries — no hardcoded fakes. Commit [`ed87f97`](https://github.com/Convospanai-outreach/fullstack/commit/ed87f97) (`apps/web/src/app/api/dashboard/summary/route.ts`). Full audit: [`dashboard_stat_cards_audit.md`](file:///C:/Users/tewar/.gemini/antigravity-ide/brain/fce46043-36d6-4142-bd27-d2bf09b2763f/dashboard_stat_cards_audit.md). |
| **OPEN-07** | Accessibility Page Audit | UX/QA | **Resolved** | Automated axe-core 4.13.0 audit run against live authenticated sessions for `/intel`, `/scraper-bridge`, `/jobs`. Full results: [`docs/audits/accessibility-intel-scraper-bridge-jobs.md`](docs/audits/accessibility-intel-scraper-bridge-jobs.md). One critical violation (`select-name` on the Scrape Target field) fixed directly (`apps/web/src/modules/scraper-bridge/ui/components/ScrapeForm.tsx` — associated the label via `htmlFor`/`id`). Seven remaining moderate/serious findings (color-contrast, landmark structure) documented in the audit file as follow-up work requiring design/layout decisions outside this pass's scope. |
| **OPEN-08** | Production Worker Process Monitoring | Operations | **Partially Resolved** | Commit [`0b23df5`](https://github.com/Convospanai-outreach/fullstack/commit/0b23df5): local supervision now defined — `apps/docker-compose.split.yml` runs the worker (`src/workers/start-workers.ts`) with `restart: unless-stopped`. Production API/worker hosting is **Render**; the database is **Neon** — not Railway/docker-compose (`docs/audits/post-pr52-production-regression-audit.md`'s "Railway" reference is stale/inaccurate). No Render credentials were available this session to verify or configure the worker service there. Commit's own message: "This does not by itself confirm a worker runs in production; the hosting platform still needs the equivalent service configured." Needs either Render access or human confirmation in the Render dashboard. |
| **OPEN-09** | Gmail PubSub / History Inbound Reply Processing Engine | Architecture | **Resolved** | Commit [`8a921a4`](https://github.com/Convospanai-outreach/fullstack/commit/8a921a4): Consolidated solely into `apps/api`'s canonical, OIDC-verified, lease-locked pipeline (`registerGoogleMailboxWatch`, `/integrations/google/pubsub` Fastify route, `gmail-history-sync-worker.ts`, `syncGoogleMailbox`). Redundant unauthenticated implementation in `apps/web` (`syncGmailInboundReplies` and `/api/integrations/google/pubsub/route.ts`) deleted per GEMINI.md §5 single source of truth rules. HTTP 400 Bad Request handling ported to `apps/api` alongside 404 for stale history ID profile resync fallback. All 282 API tests and 154 Web tests passing. |
| **OPEN-10** | Reply Event Type Mismatch (apps/api vs apps/web) | Data Integrity | **Resolved** | Commit [`8a921a4`](https://github.com/Convospanai-outreach/fullstack/commit/8a921a4): `apps/api` `createInboundCampaignEvent` wrote event type `"REPLY"` while web consumers expected `"REPLY_RECEIVED"`. Unified `apps/api` `GmailInboundEventType` and `assertCompletedInboundEvent()` check to `"REPLY_RECEIVED"`, aligned dashboard summary and funnel route filters. Redundant `apps/web` duplicate route and tests deleted. All 282 API unit tests passing (100%). |
| **OPEN-11** | `apps/api` Coverage Threshold Drift | Code Quality | **Reopened** | Was resolved at 49.07% via commit `7556801`. A separate commit, `30b372a`, then broke `npm ci` itself (missing `uuid@14.0.1` in the lockfile — see OPEN-16), so no CI run reached the coverage step for the entire window between the two commits; the coverage number was unmeasurable, not necessarily stable. Once OPEN-16 restored `npm ci` (commit `9ab6c2c`), the coverage gate ran again and now fails: `lines 48.84%` (< 49% threshold), `statements 47.98%` (< 48%). **2026-08-08:** Ruled out `prisma` version float as the cause — confirmed `prisma@7.9.1` was already present in the lockfile *before* the OPEN-16/uuid fix (`git show 0ac89aa:package-lock.json`), so it isn't a side effect of this session's lockfile edits. Most likely cause: one of the ~10 commits between `7556801` and `9ab6c2c` added `apps/api` source without matching test coverage (candidate: `8a921a4`, the Gmail PubSub consolidation, OPEN-09/OPEN-10 — it added code to `apps/api` while deleting the parallel `apps/web` implementation, which would only dilute the `apps/api` denominator). Not yet fixed — needs a human decision: add tests to close the ~0.2pp gap, exclude generated Prisma client output from instrumentation, or accept the drift and adjust the threshold. Confirmed via `gh api .../commits/9ab6c2c/check-runs` that this failure is already present on `main`, independent of PR #140. |
| **OPEN-12** | Webhook Delivery Timeout & SSRF Exposure | Security | **Resolved** | Commit [`76f84d7`](https://github.com/Convospanai-outreach/fullstack/commit/76f84d7): the `timeout: 10000` option was a no-op under undici (carried over from node-fetch), so a hung endpoint could occupy a worker indefinitely — replaced with `AbortSignal.timeout()`. User-supplied webhook URLs could probe internal infrastructure; `assertSafeWebhookUrl` now resolves the hostname and blocks loopback, RFC1918, CGNAT, link-local (incl. `169.254.169.254`), and IPv6 ULA/link-local via `net.BlockList`, checking resolved addresses (not the literal hostname) so a public DNS record can't point at an internal address. Webhook tests: 3 → 14. |
| **OPEN-13** | Dependency Advisories (Lockfile Overrides Not Applied) | Security | **Resolved, new advisories since** | Commit [`30b372a`](https://github.com/Convospanai-outreach/fullstack/commit/30b372a): the `overrides` block was never applied to the committed lockfile (`npm ci` — used by CI and both Dockerfiles — kept installing the vulnerable tree). Corrected stale pins (postcss, fast-uri, hono), added missing pins (find-my-way, ip-address, socket.io-parser, valibot), dropped the blanket brace-expansion pin, and regenerated the lockfile from a clean tree. `npm audit`: 19 vulnerabilities (10 high) → 0. **2026-08-08:** Two new advisories disclosed after that fix regressed the gate again: `dompurify` GHSA-55q2-fjhq-7xh7 (XSS) and `js-yaml` GHSA-5p4m-2wfm-xmqj / CVE-2026-59870 (DoS). Patched in PR #140 (not yet merged): `dompurify` 3.4.12 → 3.4.13, `js-yaml` 4.3.0 → 4.3.1 (root `package.json` overrides). A third advisory was found and deliberately **not** patched: `image-size` (pulled in transitively via `pptxgenjs@4.0.1`) has high-severity DoS advisories GHSA-w3rx-r6r6-pgpr and GHSA-5p2g-fcmc-qvqq covering `range: "<=2.0.2"` — i.e. no patched version of `image-size` exists at all. The only remedy `npm audit fix --force` offers is downgrading `pptxgenjs` 4.0.1 → 1.1.5, a breaking change that would regress the live PPTX-export feature. Left alone pending a product decision (accept the risk / downgrade and lose functionality / wait for upstream patch). |
| **OPEN-14** | npm audit CI Gate Non-Blocking | CI/CD | **Resolved, currently red** | Commit [`df90374`](https://github.com/Convospanai-outreach/fullstack/commit/df90374): the audit step ran with `\|\| true`, so it could never fail a build. Now blocking; verified `npm audit --audit-level=high --omit=dev` exits 0 against the tree at the time. The destructive-migration scan is deliberately left advisory (34 findings, 4 critical, in migrations already applied to production — needs an allowlist for applied history before it can gate). **2026-08-08:** Because the gate is now correctly blocking, the new `image-size` advisory in OPEN-13 currently fails this gate on `main` and on PR #140 until that product decision is made. |
| **OPEN-15** | Deactivated/Invited Users Retaining Team Context | Security | **Resolved** | Commit [`2c00d25`](https://github.com/Convospanai-outreach/fullstack/commit/2c00d25): `getCurrentContext()` matched team membership on `userId`(+`teamId`) with no status filter, so a deactivated user holding a live session retained full team context (same for invited-but-never-joined rows). `apps/api` was fully exposed — it has no Clerk code path, so every request resolved context through these lookups. All four lookups (both apps) now constrain to `status: "active"`; regression tests added, verified to fail without the fix. Also removed unreachable `CredentialsProvider`/Google provider dead code from `apps/api` authOptions (verified nothing reads `authOptions.providers` there — apps/api is Clerk-only via a proxy to apps/web). |
| **OPEN-16** | `npm ci` Broken (Missing `uuid@14.0.1` in Lockfile) | CI/CD | **Resolved** | Commit [`9ab6c2c`](https://github.com/Convospanai-outreach/fullstack/commit/9ab6c2c) (PR #139): a prior lockfile edit left `uuid@14.0.1` (pulled in via the root `overrides` pin) missing from the `apps/api`/`apps/web` workspace `node_modules/uuid` entries, so `npm ci` failed with "Missing: uuid@14.0.1 from lock file" and blocked every CI job and both Docker builds. Regenerated with `npm@10.9.8` (matching CI's Node 22.x-bundled npm — local npm 11.6.3 resolves workspace overrides differently and produces a lockfile CI can't consume) and hand-verified the diff was a pure 52-line addition. Unintended side effect: `apps/api`'s `"prisma": "^7.8.0"` caret range let this regeneration float the resolved `prisma`/`@prisma/*` family to `7.9.1` — noted for anyone chasing OPEN-11. |
| **OPEN-17** | Untraced Auto-Merge Automation on this Repo | CI/CD / Security | **Still Open** | An automation auto-creates and auto-merges PRs within ~30-60s of a push landing, acting under the user's own GitHub identity (`Convospanai-outreach`). This happened to PR #138 and PR #139 during this session, in both cases merging a first, incomplete commit to `main` before a corrective follow-up could be pushed to the same branch — meaning any multi-commit PR risks landing a known-broken intermediate state on `main`. The repo's `allow_auto_merge` setting is `false`, and no matching GitHub Actions workflow, branch protection rule, or webhook was found to explain it. Flagged to the user twice; unresolved. Until found and disabled, treat every push to this repo as functionally equivalent to a direct push to `main`, and land fixes as single, pre-verified commits on fresh branches rather than iterating on an already-pushed one. |
